<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default: cleartext HTTP disabled for security (bank-grade HTTPS enforcement) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Cleartext HTTP permitted only for local development & staging emulator/LAN hosts -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android Emulator host loopback -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- Localhost loopback -->
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- Common local development LAN IP subnet -->
        <domain includeSubdomains="true">192.168.29.46</domain>
    </domain-config>
</network-security-config>
